module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // Only add cssnano for production builds to minimize CSS
    ...(process.env.NODE_ENV === 'production'
      ? {
          cssnano: {
            preset: [
              'default',
              {
                // Disable discardComments to preserve important comments
                discardComments: { removeAll: true },
                // Normalize and minify
                normalizeWhitespace: true,
                mergeLonghand: true,
              },
            ],
          },
        }
      : {}),
  },
};
